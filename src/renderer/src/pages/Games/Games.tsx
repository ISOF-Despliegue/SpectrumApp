import styles from './Home.module.css';
import { useTranslation } from 'react-i18next';
import { GlassContainer } from '@renderer/components/ui/GlassContainer';

export const Games = () => {

  /*const { textCommon } = useTranslation('common');
  const { textGames } = useTranslation('games');*/

    return (
      <div>
        <h1>Bienvenido a la sección de Juegos</h1>
      </div>
    );

};
